// A simple test file to check if Jest is working

describe('Basic tests', () => {
  test('addition works', () => {
    expect(1 + 1).toBe(2);
  });
  
  test('string concatenation works', () => {
    expect('hello' + ' world').toBe('hello world');
  });
}); 